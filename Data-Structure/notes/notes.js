// Data structure allows up to talk about how the runtime of an algorithm grows  as the imput grows

function addUp(n) {
  var total = 0;
  for (var i = 0; i < n; i++) {
    total = +i;
  }
  return total;
}

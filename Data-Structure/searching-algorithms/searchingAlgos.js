/*
Objectives
1. Linear Search
2. Binary Search
3. Naive searching algos
4. KMP searching algos
*/

/* 
    LINEAR SEARCH
    is going thru all the element  in an array check and make comparasion what you are looking for!
    -> some js methods --namely indexOf, includes, find, findIndex,  do the linear search behind the schene.

    function linearSearch(arr, value) {
  for (let key in arr) {
    if (arr[key] === value) return key;
  }
  return -1;
}

console.log(linearSearch([2, 3, 5, 6, 8, 9], 7));
    
*/

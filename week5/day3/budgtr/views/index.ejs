<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Budget Index Page</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css"></script>
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/skeleton.css">
    <style>
        .budget-container {
            width: 80vw;
            margin: 10px auto 0 auto;
        }

        button {
            position: absolute;
            right: 60px;
            top: 40px;
        }
    </style>
</head>

<body>
    <div class="budget-container">
        <h1>Budgtr</h1>
        <button class="button-primary" onclick="location.href='/budget/new'" type="button">ADD A NEW ITEM</button>
        <table class="u-full-width">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Name</th>
                    <th>Amount</th>
                </tr>
            </thead>
            <tbody>

                <% for(let i=0;i<Budget.length; i++){ %>
                <tr>
                    <td><%= Budget[i].date %></td>
                    <td><a href="/index/<%= i %>"><%= Budget[i].name %></a></td>
                    <td><%= Budget[i].amount %></td>
                </tr>
                <% } %>

            </tbody>
        </table>
    </div>
</body>

</html>